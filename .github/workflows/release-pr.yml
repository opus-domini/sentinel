name: Prepare Release PR

on:
  push:
    branches:
      - main

jobs:
  prepare:
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses: opus-domini/actions/.github/workflows/release-pr.yml@0f492b1146df1f2316d2bb6b69b5bbd34c6548f1
