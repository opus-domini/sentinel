name: Prepare Release PR

on:
  push:
    branches:
      - main

jobs:
  prepare:
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses: opus-domini/actions/.github/workflows/release-pr.yml@2a77045ce3f8ac6a7e9ba476cd2a0a6ea5c2b883
