name: Weekly Vulnerability Scan

on:
  schedule:
    # Organization scans use distinct minutes to avoid a synchronized burst.
    - cron: "37 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vuln:
    uses: opus-domini/actions/.github/workflows/vulnerability-scan.yml@8076cfe3446015fb5b14aec84aa7e69cd231bd37
