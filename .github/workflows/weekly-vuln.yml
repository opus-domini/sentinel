name: Weekly Vulnerability Scan

on:
  schedule:
    # Organization scans use distinct minutes to avoid a synchronized burst.
    - cron: "37 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vuln:
    uses: opus-domini/actions/.github/workflows/vulnerability-scan.yml@61e7db8e88d62cef98221ec83c8a6175381fce91
