name: Weekly Vulnerability Scan

on:
  schedule:
    # Organization scans use distinct minutes to avoid a synchronized burst.
    - cron: "37 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vuln:
    uses: opus-domini/actions/.github/workflows/vulnerability-scan.yml@465a0c4e914bbaad723cc08170d7b918f675a841
