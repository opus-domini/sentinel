name: Weekly Vulnerability Scan

on:
  schedule:
    # Organization scans use distinct minutes to avoid a synchronized burst.
    - cron: "37 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vuln:
    uses: opus-domini/actions/.github/workflows/vulnerability-scan.yml@17196e480ef7b110e3c3afe624157600689f4661
