name: Weekly Vulnerability Scan

on:
  schedule:
    # Organization scans use distinct minutes to avoid a synchronized burst.
    - cron: "37 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vuln:
    uses: opus-domini/actions/.github/workflows/vulnerability-scan.yml@372c1c178b6721176b31b9cb61e09dd3a7714383
