name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: opus-domini/actions/.github/workflows/ci.yml@d46c516d62240b7b6a10bb3e4caf2a1810e8d8a9
    with:
      frontend: true
      upload-codecov: true
