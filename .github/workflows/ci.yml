name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: opus-domini/actions/.github/workflows/ci.yml@1bc7bd95911fd11f6049c1c9b8a379aed73983e6
    with:
      frontend: true
      upload-codecov: true
