name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: opus-domini/actions/.github/workflows/ci.yml@00f37fef202600f60a11587a05474d1239cd48fc
    with:
      frontend: true
      upload-codecov: true
