name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: opus-domini/actions/.github/workflows/ci.yml@0445ca444daaf1b13cbc4c28f306784fb8de08ef
    with:
      frontend: true
      upload-codecov: true
