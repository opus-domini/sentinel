name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: opus-domini/actions/.github/workflows/ci.yml@e61ccf4381caa98150bdc8bd8d9ea791235a6691
    with:
      frontend: true
      upload-codecov: true
